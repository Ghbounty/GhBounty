/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { GHBOUNTY_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const REFUND_STAKE_DEPOSIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([215, 251, 81, 108, 221, 232, 147, 0]);

export function getRefundStakeDepositDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_STAKE_DEPOSIT_DISCRIMINATOR,
  );
}

export type RefundStakeDepositInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountStake extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountStake extends string
        ? WritableAccount<TAccountStake>
        : TAccountStake,
      TAccountOwner extends string
        ? WritableAccount<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type RefundStakeDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RefundStakeDepositInstructionDataArgs = {};

export function getRefundStakeDepositInstructionDataEncoder(): FixedSizeEncoder<RefundStakeDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REFUND_STAKE_DEPOSIT_DISCRIMINATOR,
    }),
  );
}

export function getRefundStakeDepositInstructionDataDecoder(): FixedSizeDecoder<RefundStakeDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundStakeDepositInstructionDataCodec(): FixedSizeCodec<
  RefundStakeDepositInstructionDataArgs,
  RefundStakeDepositInstructionData
> {
  return combineCodec(
    getRefundStakeDepositInstructionDataEncoder(),
    getRefundStakeDepositInstructionDataDecoder(),
  );
}

export type RefundStakeDepositInput<
  TAccountAuthority extends string = string,
  TAccountStake extends string = string,
  TAccountOwner extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  stake: Address<TAccountStake>;
  owner: Address<TAccountOwner>;
};

export function getRefundStakeDepositInstruction<
  TAccountAuthority extends string,
  TAccountStake extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: RefundStakeDepositInput<
    TAccountAuthority,
    TAccountStake,
    TAccountOwner
  >,
  config?: { programAddress?: TProgramAddress },
): RefundStakeDepositInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountStake,
  TAccountOwner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    stake: { value: input.stake ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getRefundStakeDepositInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundStakeDepositInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStake,
    TAccountOwner
  >);
}

export type ParsedRefundStakeDepositInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    stake: TAccountMetas[1];
    owner: TAccountMetas[2];
  };
  data: RefundStakeDepositInstructionData;
};

export function parseRefundStakeDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundStakeDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      stake: getNextAccount(),
      owner: getNextAccount(),
    },
    data: getRefundStakeDepositInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
