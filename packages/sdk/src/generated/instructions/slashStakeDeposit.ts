/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { GHBOUNTY_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const SLASH_STAKE_DEPOSIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([121, 156, 0, 205, 106, 248, 58, 74]);

export function getSlashStakeDepositDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SLASH_STAKE_DEPOSIT_DISCRIMINATOR,
  );
}

export type SlashStakeDepositInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountStake extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountStake extends string
        ? WritableAccount<TAccountStake>
        : TAccountStake,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      ...TRemainingAccounts,
    ]
  >;

export type SlashStakeDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type SlashStakeDepositInstructionDataArgs = { amount: number | bigint };

export function getSlashStakeDepositInstructionDataEncoder(): FixedSizeEncoder<SlashStakeDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SLASH_STAKE_DEPOSIT_DISCRIMINATOR }),
  );
}

export function getSlashStakeDepositInstructionDataDecoder(): FixedSizeDecoder<SlashStakeDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getSlashStakeDepositInstructionDataCodec(): FixedSizeCodec<
  SlashStakeDepositInstructionDataArgs,
  SlashStakeDepositInstructionData
> {
  return combineCodec(
    getSlashStakeDepositInstructionDataEncoder(),
    getSlashStakeDepositInstructionDataDecoder(),
  );
}

export type SlashStakeDepositInput<
  TAccountAuthority extends string = string,
  TAccountStake extends string = string,
  TAccountTreasury extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  stake: Address<TAccountStake>;
  /** (the relayer always uses the GhBounty slash treasury account). */
  treasury: Address<TAccountTreasury>;
  amount: SlashStakeDepositInstructionDataArgs["amount"];
};

export function getSlashStakeDepositInstruction<
  TAccountAuthority extends string,
  TAccountStake extends string,
  TAccountTreasury extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: SlashStakeDepositInput<
    TAccountAuthority,
    TAccountStake,
    TAccountTreasury
  >,
  config?: { programAddress?: TProgramAddress },
): SlashStakeDepositInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountStake,
  TAccountTreasury
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    stake: { value: input.stake ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("treasury", accounts.treasury),
    ],
    data: getSlashStakeDepositInstructionDataEncoder().encode(
      args as SlashStakeDepositInstructionDataArgs,
    ),
    programAddress,
  } as SlashStakeDepositInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStake,
    TAccountTreasury
  >);
}

export type ParsedSlashStakeDepositInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    stake: TAccountMetas[1];
    /** (the relayer always uses the GhBounty slash treasury account). */
    treasury: TAccountMetas[2];
  };
  data: SlashStakeDepositInstructionData;
};

export function parseSlashStakeDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSlashStakeDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      stake: getNextAccount(),
      treasury: getNextAccount(),
    },
    data: getSlashStakeDepositInstructionDataDecoder().decode(instruction.data),
  };
}
