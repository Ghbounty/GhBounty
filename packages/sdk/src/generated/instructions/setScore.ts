/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { GHBOUNTY_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const SET_SCORE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  218, 167, 25, 121, 208, 190, 8, 87,
]);

export function getSetScoreDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_SCORE_DISCRIMINATOR);
}

export type SetScoreInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountScorer extends string | AccountMeta<string> = string,
  TAccountBounty extends string | AccountMeta<string> = string,
  TAccountSubmission extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountScorer extends string
        ? ReadonlySignerAccount<TAccountScorer> &
            AccountSignerMeta<TAccountScorer>
        : TAccountScorer,
      TAccountBounty extends string
        ? ReadonlyAccount<TAccountBounty>
        : TAccountBounty,
      TAccountSubmission extends string
        ? WritableAccount<TAccountSubmission>
        : TAccountSubmission,
      ...TRemainingAccounts,
    ]
  >;

export type SetScoreInstructionData = {
  discriminator: ReadonlyUint8Array;
  score: number;
};

export type SetScoreInstructionDataArgs = { score: number };

export function getSetScoreInstructionDataEncoder(): FixedSizeEncoder<SetScoreInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["score", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_SCORE_DISCRIMINATOR }),
  );
}

export function getSetScoreInstructionDataDecoder(): FixedSizeDecoder<SetScoreInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["score", getU8Decoder()],
  ]);
}

export function getSetScoreInstructionDataCodec(): FixedSizeCodec<
  SetScoreInstructionDataArgs,
  SetScoreInstructionData
> {
  return combineCodec(
    getSetScoreInstructionDataEncoder(),
    getSetScoreInstructionDataDecoder(),
  );
}

export type SetScoreInput<
  TAccountScorer extends string = string,
  TAccountBounty extends string = string,
  TAccountSubmission extends string = string,
> = {
  scorer: TransactionSigner<TAccountScorer>;
  bounty: Address<TAccountBounty>;
  submission: Address<TAccountSubmission>;
  score: SetScoreInstructionDataArgs["score"];
};

export function getSetScoreInstruction<
  TAccountScorer extends string,
  TAccountBounty extends string,
  TAccountSubmission extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: SetScoreInput<TAccountScorer, TAccountBounty, TAccountSubmission>,
  config?: { programAddress?: TProgramAddress },
): SetScoreInstruction<
  TProgramAddress,
  TAccountScorer,
  TAccountBounty,
  TAccountSubmission
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    scorer: { value: input.scorer ?? null, isWritable: false },
    bounty: { value: input.bounty ?? null, isWritable: false },
    submission: { value: input.submission ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("scorer", accounts.scorer),
      getAccountMeta("bounty", accounts.bounty),
      getAccountMeta("submission", accounts.submission),
    ],
    data: getSetScoreInstructionDataEncoder().encode(
      args as SetScoreInstructionDataArgs,
    ),
    programAddress,
  } as SetScoreInstruction<
    TProgramAddress,
    TAccountScorer,
    TAccountBounty,
    TAccountSubmission
  >);
}

export type ParsedSetScoreInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    scorer: TAccountMetas[0];
    bounty: TAccountMetas[1];
    submission: TAccountMetas[2];
  };
  data: SetScoreInstructionData;
};

export function parseSetScoreInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetScoreInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      scorer: getNextAccount(),
      bounty: getNextAccount(),
      submission: getNextAccount(),
    },
    data: getSetScoreInstructionDataDecoder().decode(instruction.data),
  };
}
