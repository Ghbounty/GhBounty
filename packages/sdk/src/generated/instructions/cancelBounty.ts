/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { GHBOUNTY_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_BOUNTY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  79, 65, 107, 143, 128, 165, 135, 46,
]);

export function getCancelBountyDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_BOUNTY_DISCRIMINATOR,
  );
}

export type CancelBountyInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountBounty extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountBounty extends string
        ? WritableAccount<TAccountBounty>
        : TAccountBounty,
      ...TRemainingAccounts,
    ]
  >;

export type CancelBountyInstructionData = { discriminator: ReadonlyUint8Array };

export type CancelBountyInstructionDataArgs = {};

export function getCancelBountyInstructionDataEncoder(): FixedSizeEncoder<CancelBountyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_BOUNTY_DISCRIMINATOR }),
  );
}

export function getCancelBountyInstructionDataDecoder(): FixedSizeDecoder<CancelBountyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelBountyInstructionDataCodec(): FixedSizeCodec<
  CancelBountyInstructionDataArgs,
  CancelBountyInstructionData
> {
  return combineCodec(
    getCancelBountyInstructionDataEncoder(),
    getCancelBountyInstructionDataDecoder(),
  );
}

export type CancelBountyInput<
  TAccountCreator extends string = string,
  TAccountBounty extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  bounty: Address<TAccountBounty>;
};

export function getCancelBountyInstruction<
  TAccountCreator extends string,
  TAccountBounty extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: CancelBountyInput<TAccountCreator, TAccountBounty>,
  config?: { programAddress?: TProgramAddress },
): CancelBountyInstruction<TProgramAddress, TAccountCreator, TAccountBounty> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    bounty: { value: input.bounty ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("bounty", accounts.bounty),
    ],
    data: getCancelBountyInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelBountyInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountBounty
  >);
}

export type ParsedCancelBountyInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    bounty: TAccountMetas[1];
  };
  data: CancelBountyInstructionData;
};

export function parseCancelBountyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelBountyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { creator: getNextAccount(), bounty: getNextAccount() },
    data: getCancelBountyInstructionDataDecoder().decode(instruction.data),
  };
}
