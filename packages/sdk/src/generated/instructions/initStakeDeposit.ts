/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findStakePda } from "../pdas";
import { GHBOUNTY_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const INIT_STAKE_DEPOSIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([213, 203, 209, 208, 0, 230, 132, 106]);

export function getInitStakeDepositDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_STAKE_DEPOSIT_DISCRIMINATOR,
  );
}

export type InitStakeDepositInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountStake extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountStake extends string
        ? WritableAccount<TAccountStake>
        : TAccountStake,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitStakeDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type InitStakeDepositInstructionDataArgs = { amount: number | bigint };

export function getInitStakeDepositInstructionDataEncoder(): FixedSizeEncoder<InitStakeDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_STAKE_DEPOSIT_DISCRIMINATOR }),
  );
}

export function getInitStakeDepositInstructionDataDecoder(): FixedSizeDecoder<InitStakeDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getInitStakeDepositInstructionDataCodec(): FixedSizeCodec<
  InitStakeDepositInstructionDataArgs,
  InitStakeDepositInstructionData
> {
  return combineCodec(
    getInitStakeDepositInstructionDataEncoder(),
    getInitStakeDepositInstructionDataDecoder(),
  );
}

export type InitStakeDepositAsyncInput<
  TAccountOwner extends string = string,
  TAccountStake extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  stake?: Address<TAccountStake>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: InitStakeDepositInstructionDataArgs["amount"];
};

export async function getInitStakeDepositInstructionAsync<
  TAccountOwner extends string,
  TAccountStake extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: InitStakeDepositAsyncInput<
    TAccountOwner,
    TAccountStake,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitStakeDepositInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountStake,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    stake: { value: input.stake ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.stake.value) {
    accounts.stake.value = await findStakePda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitStakeDepositInstructionDataEncoder().encode(
      args as InitStakeDepositInstructionDataArgs,
    ),
    programAddress,
  } as InitStakeDepositInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountStake,
    TAccountSystemProgram
  >);
}

export type InitStakeDepositInput<
  TAccountOwner extends string = string,
  TAccountStake extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  stake: Address<TAccountStake>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: InitStakeDepositInstructionDataArgs["amount"];
};

export function getInitStakeDepositInstruction<
  TAccountOwner extends string,
  TAccountStake extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: InitStakeDepositInput<
    TAccountOwner,
    TAccountStake,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitStakeDepositInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountStake,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    stake: { value: input.stake ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitStakeDepositInstructionDataEncoder().encode(
      args as InitStakeDepositInstructionDataArgs,
    ),
    programAddress,
  } as InitStakeDepositInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountStake,
    TAccountSystemProgram
  >);
}

export type ParsedInitStakeDepositInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    stake: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitStakeDepositInstructionData;
};

export function parseInitStakeDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitStakeDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      stake: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitStakeDepositInstructionDataDecoder().decode(instruction.data),
  };
}
