/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { GHBOUNTY_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const RESOLVE_BOUNTY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  207, 43, 93, 238, 222, 184, 79, 219,
]);

export function getResolveBountyDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESOLVE_BOUNTY_DISCRIMINATOR,
  );
}

export type ResolveBountyInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountBounty extends string | AccountMeta<string> = string,
  TAccountWinningSubmission extends string | AccountMeta<string> = string,
  TAccountWinner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountBounty extends string
        ? WritableAccount<TAccountBounty>
        : TAccountBounty,
      TAccountWinningSubmission extends string
        ? WritableAccount<TAccountWinningSubmission>
        : TAccountWinningSubmission,
      TAccountWinner extends string
        ? WritableAccount<TAccountWinner>
        : TAccountWinner,
      ...TRemainingAccounts,
    ]
  >;

export type ResolveBountyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ResolveBountyInstructionDataArgs = {};

export function getResolveBountyInstructionDataEncoder(): FixedSizeEncoder<ResolveBountyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESOLVE_BOUNTY_DISCRIMINATOR }),
  );
}

export function getResolveBountyInstructionDataDecoder(): FixedSizeDecoder<ResolveBountyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResolveBountyInstructionDataCodec(): FixedSizeCodec<
  ResolveBountyInstructionDataArgs,
  ResolveBountyInstructionData
> {
  return combineCodec(
    getResolveBountyInstructionDataEncoder(),
    getResolveBountyInstructionDataDecoder(),
  );
}

export type ResolveBountyInput<
  TAccountCreator extends string = string,
  TAccountBounty extends string = string,
  TAccountWinningSubmission extends string = string,
  TAccountWinner extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  bounty: Address<TAccountBounty>;
  winningSubmission: Address<TAccountWinningSubmission>;
  winner: Address<TAccountWinner>;
};

export function getResolveBountyInstruction<
  TAccountCreator extends string,
  TAccountBounty extends string,
  TAccountWinningSubmission extends string,
  TAccountWinner extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: ResolveBountyInput<
    TAccountCreator,
    TAccountBounty,
    TAccountWinningSubmission,
    TAccountWinner
  >,
  config?: { programAddress?: TProgramAddress },
): ResolveBountyInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountBounty,
  TAccountWinningSubmission,
  TAccountWinner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: false },
    bounty: { value: input.bounty ?? null, isWritable: true },
    winningSubmission: {
      value: input.winningSubmission ?? null,
      isWritable: true,
    },
    winner: { value: input.winner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("bounty", accounts.bounty),
      getAccountMeta("winningSubmission", accounts.winningSubmission),
      getAccountMeta("winner", accounts.winner),
    ],
    data: getResolveBountyInstructionDataEncoder().encode({}),
    programAddress,
  } as ResolveBountyInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountBounty,
    TAccountWinningSubmission,
    TAccountWinner
  >);
}

export type ParsedResolveBountyInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    bounty: TAccountMetas[1];
    winningSubmission: TAccountMetas[2];
    winner: TAccountMetas[3];
  };
  data: ResolveBountyInstructionData;
};

export function parseResolveBountyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResolveBountyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      bounty: getNextAccount(),
      winningSubmission: getNextAccount(),
      winner: getNextAccount(),
    },
    data: getResolveBountyInstructionDataDecoder().decode(instruction.data),
  };
}
