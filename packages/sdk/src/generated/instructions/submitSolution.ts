/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { GHBOUNTY_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const SUBMIT_SOLUTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [203, 233, 157, 191, 70, 37, 205, 0],
);

export function getSubmitSolutionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_SOLUTION_DISCRIMINATOR,
  );
}

export type SubmitSolutionInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountSolver extends string | AccountMeta<string> = string,
  TAccountBounty extends string | AccountMeta<string> = string,
  TAccountSubmission extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSolver extends string
        ? WritableSignerAccount<TAccountSolver> &
            AccountSignerMeta<TAccountSolver>
        : TAccountSolver,
      TAccountBounty extends string
        ? WritableAccount<TAccountBounty>
        : TAccountBounty,
      TAccountSubmission extends string
        ? WritableAccount<TAccountSubmission>
        : TAccountSubmission,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitSolutionInstructionData = {
  discriminator: ReadonlyUint8Array;
  prUrl: string;
  opusReportHash: ReadonlyUint8Array;
};

export type SubmitSolutionInstructionDataArgs = {
  prUrl: string;
  opusReportHash: ReadonlyUint8Array;
};

export function getSubmitSolutionInstructionDataEncoder(): Encoder<SubmitSolutionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["prUrl", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["opusReportHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_SOLUTION_DISCRIMINATOR }),
  );
}

export function getSubmitSolutionInstructionDataDecoder(): Decoder<SubmitSolutionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["prUrl", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["opusReportHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getSubmitSolutionInstructionDataCodec(): Codec<
  SubmitSolutionInstructionDataArgs,
  SubmitSolutionInstructionData
> {
  return combineCodec(
    getSubmitSolutionInstructionDataEncoder(),
    getSubmitSolutionInstructionDataDecoder(),
  );
}

export type SubmitSolutionInput<
  TAccountSolver extends string = string,
  TAccountBounty extends string = string,
  TAccountSubmission extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  solver: TransactionSigner<TAccountSolver>;
  bounty: Address<TAccountBounty>;
  submission: Address<TAccountSubmission>;
  systemProgram?: Address<TAccountSystemProgram>;
  prUrl: SubmitSolutionInstructionDataArgs["prUrl"];
  opusReportHash: SubmitSolutionInstructionDataArgs["opusReportHash"];
};

export function getSubmitSolutionInstruction<
  TAccountSolver extends string,
  TAccountBounty extends string,
  TAccountSubmission extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
>(
  input: SubmitSolutionInput<
    TAccountSolver,
    TAccountBounty,
    TAccountSubmission,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitSolutionInstruction<
  TProgramAddress,
  TAccountSolver,
  TAccountBounty,
  TAccountSubmission,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHBOUNTY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solver: { value: input.solver ?? null, isWritable: true },
    bounty: { value: input.bounty ?? null, isWritable: true },
    submission: { value: input.submission ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("solver", accounts.solver),
      getAccountMeta("bounty", accounts.bounty),
      getAccountMeta("submission", accounts.submission),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSubmitSolutionInstructionDataEncoder().encode(
      args as SubmitSolutionInstructionDataArgs,
    ),
    programAddress,
  } as SubmitSolutionInstruction<
    TProgramAddress,
    TAccountSolver,
    TAccountBounty,
    TAccountSubmission,
    TAccountSystemProgram
  >);
}

export type ParsedSubmitSolutionInstruction<
  TProgram extends string = typeof GHBOUNTY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    solver: TAccountMetas[0];
    bounty: TAccountMetas[1];
    submission: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SubmitSolutionInstructionData;
};

export function parseSubmitSolutionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitSolutionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      solver: getNextAccount(),
      bounty: getNextAccount(),
      submission: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSubmitSolutionInstructionDataDecoder().decode(instruction.data),
  };
}
